# SQL migrations live here (golang-migrate format).
# The shared database schema is owned by ADR-013 (web drizzle) and applied
# via psql; gateway-specific migrations are added as they are introduced.
